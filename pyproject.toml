[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["agents", "config", "document_processor", "retriever", "utils"]

[tool.pytest.ini_options]
testpaths = ["tests"]
